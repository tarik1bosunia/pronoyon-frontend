# 📦 Frontend Libraries for Question Editor UI

Complete guide for building a question editor interface with rich text editing capabilities.

---

## 🎨 **Core UI Libraries**

### **1. Rich Text Editor (Main Requirement)**

**Option A: Tiptap (Recommended)**
```bash
npm install @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-image @tiptap/extension-table @tiptap/extension-table-row @tiptap/extension-table-cell @tiptap/extension-table-header @tiptap/extension-underline @tiptap/extension-text-align @tiptap/extension-placeholder
```

**Features:**
- ✅ Toolbar with Bold, Italic, Underline, Headings
- ✅ Code blocks, Images, Tables
- ✅ LaTeX/Math support
- ✅ Highly customizable toolbar
- ✅ Perfect for your use case

**Option B: Lexical (Facebook's Editor)**
```bash
npm install lexical @lexical/react
```

**Option C: Slate.js**
```bash
npm install slate slate-react
```

---

### **2. Form Management**

```bash
npm install react-hook-form zod @hookform/resolvers
```

**Purpose:**
- Handle form state (questions, options, points)
- Validation
- Dynamic fields (add/remove options)

---

### **3. Drag & Drop (for reordering options)**

```bash
npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
```

**Or simpler:**
```bash
npm install react-beautiful-dnd
```

---

### **4. UI Components (shadcn/ui already included)**

You already have shadcn/ui, which provides:
- ✅ Input fields
- ✅ Buttons
- ✅ Select dropdowns
- ✅ Radio buttons
- ✅ Icons (via lucide-react)

Additional components you might need:
```bash
npx shadcn-ui@latest add input
npx shadcn-ui@latest add button
npx shadcn-ui@latest add select
npx shadcn-ui@latest add radio-group
npx shadcn-ui@latest add textarea
npx shadcn-ui@latest add label
npx shadcn-ui@latest add separator
npx shadcn-ui@latest add card
npx shadcn-ui@latest add badge
```

---

## 🔧 **Complete Package List**

```json
{
  "dependencies": {
    "next": "14.1.0",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "typescript": "5.3.3",
    
    "@reduxjs/toolkit": "2.1.0",
    "react-redux": "9.1.0",
    
    "tailwindcss": "3.4.1",
    "class-variance-authority": "0.7.0",
    "clsx": "2.1.0",
    "tailwind-merge": "2.2.1",
    
    "@tiptap/react": "2.1.16",
    "@tiptap/starter-kit": "2.1.16",
    "@tiptap/extension-link": "2.1.16",
    "@tiptap/extension-image": "2.1.16",
    "@tiptap/extension-table": "2.1.16",
    "@tiptap/extension-table-row": "2.1.16",
    "@tiptap/extension-table-cell": "2.1.16",
    "@tiptap/extension-table-header": "2.1.16",
    "@tiptap/extension-underline": "2.1.16",
    "@tiptap/extension-text-align": "2.1.16",
    "@tiptap/extension-placeholder": "2.1.16",
    "@tiptap/extension-mathematics": "2.1.16",
    
    "react-hook-form": "7.49.3",
    "zod": "3.22.4",
    "@hookform/resolvers": "3.3.4",
    
    "@dnd-kit/core": "6.1.0",
    "@dnd-kit/sortable": "8.0.0",
    "@dnd-kit/utilities": "3.2.2",
    
    "lucide-react": "0.316.0",
    
    "react-markdown": "9.0.1",
    "remark-gfm": "4.0.0",
    "remark-math": "6.0.0",
    "rehype-katex": "7.0.0",
    "katex": "0.16.9",
    
    "axios": "1.6.7",
    "date-fns": "3.3.1"
  }
}
```

---

## 💻 **Implementation Example**

### **QuestionEditor Component**

```typescript
'use client';

import { useState } from 'react';
import { useForm, useFieldArray } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import * as z from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { RadioGroup, RadioGroupItem } from '@/components/ui/radio-group';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Card } from '@/components/ui/card';
import { Trash2, Plus } from 'lucide-react';
import RichTextEditor from './RichTextEditor';

// Validation Schema
const questionSchema = z.object({
  title: z.string().min(1, "Title is required"),
  source: z.string().optional(),
  points: z.number().min(0),
  questionType: z.enum(['mcq', 'cq', 'written']),
  questionText: z.string().min(1, "Question text is required"),
  options: z.array(z.object({
    text: z.string().min(1),
    isCorrect: z.boolean(),
  })).min(2).max(5),
});

type QuestionFormData = z.infer<typeof questionSchema>;

export default function QuestionEditor() {
  const { register, control, handleSubmit, watch, formState: { errors } } = useForm<QuestionFormData>({
    resolver: zodResolver(questionSchema),
    defaultValues: {
      title: '',
      source: '',
      points: 1,
      questionType: 'mcq',
      questionText: '',
      options: [
        { text: '', isCorrect: false },
        { text: '', isCorrect: false },
        { text: '', isCorrect: false },
        { text: '', isCorrect: false },
      ],
    },
  });

  const { fields, append, remove } = useFieldArray({
    control,
    name: "options",
  });

  const onSubmit = (data: QuestionFormData) => {
    console.log(data);
    // Send to API
  };

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="max-w-5xl mx-auto p-6 space-y-6">
      {/* Header Section */}
      <Card className="p-4">
        <div className="flex items-center gap-4">
          <div className="flex-1">
            <Label>Question Number</Label>
            <Input type="number" defaultValue={1} className="w-20" />
          </div>
          
          <div className="flex-1">
            <Label>Source</Label>
            <Input {...register('source')} placeholder="Source" />
          </div>
          
          <div className="flex-1">
            <Label>Points</Label>
            <Input type="number" {...register('points', { valueAsNumber: true })} defaultValue={1} className="w-20" />
          </div>
          
          <div className="flex-1">
            <Label>Question Type</Label>
            <Select defaultValue="mcq">
              <SelectTrigger>
                <SelectValue />
              </SelectTrigger>
              <SelectContent>
                <SelectItem value="mcq">MCQ</SelectItem>
                <SelectItem value="cq">CQ</SelectItem>
                <SelectItem value="written">Written</SelectItem>
              </SelectContent>
            </Select>
          </div>
          
          <Button type="button" variant="destructive" size="sm">
            Delete
          </Button>
        </div>
      </Card>

      {/* Question Title */}
      <div>
        <Input
          {...register('title')}
          placeholder="Enter question title"
          className="text-lg font-medium"
        />
      </div>

      {/* Rich Text Editor for Question */}
      <div className="bg-blue-50 p-4 rounded-lg">
        <Label className="text-sm font-semibold mb-2 block">Question</Label>
        <RichTextEditor
          content=""
          onChange={(content) => {
            // Handle content change
          }}
        />
      </div>

      {/* Options Section */}
      <div className="space-y-3">
        {fields.map((field, index) => (
          <div key={field.id} className="flex items-start gap-3">
            <RadioGroupItem
              value={`option-${index}`}
              className="mt-3"
            />
            <div className="flex-1 flex items-center gap-2 p-3 border rounded-lg bg-gray-50">
              <span className="font-semibold">
                {String.fromCharCode(65 + index)}
              </span>
              <Input
                {...register(`options.${index}.text`)}
                placeholder={`Option ${String.fromCharCode(65 + index)}`}
                className="flex-1"
              />
            </div>
            {fields.length > 2 && (
              <Button
                type="button"
                variant="ghost"
                size="sm"
                onClick={() => remove(index)}
              >
                <Trash2 className="w-4 h-4" />
              </Button>
            )}
          </div>
        ))}
      </div>

      {/* Add Option Button */}
      {fields.length < 5 && (
        <Button
          type="button"
          variant="outline"
          onClick={() => append({ text: '', isCorrect: false })}
          className="w-full"
        >
          <Plus className="w-4 h-4 mr-2" />
          Add Option
        </Button>
      )}

      {/* Solution Section */}
      <div className="bg-green-50 p-4 rounded-lg">
        <Label className="text-sm font-semibold mb-2 block">Solution</Label>
        <RichTextEditor
          content=""
          onChange={(content) => {
            // Handle solution content
          }}
        />
      </div>

      {/* Action Buttons */}
      <div className="flex gap-2">
        <Button type="button" variant="outline">
          ADD
        </Button>
        <Button type="button" variant="outline">
          ADD 5
        </Button>
        <Button type="submit">
          Save Question
        </Button>
      </div>
    </form>
  );
}
```

---

### **RichTextEditor Component (Tiptap)**

```typescript
'use client';

import { useEditor, EditorContent } from '@tiptap/react';
import StarterKit from '@tiptap/starter-kit';
import Underline from '@tiptap/extension-underline';
import Link from '@tiptap/extension-link';
import Image from '@tiptap/extension-image';
import Table from '@tiptap/extension-table';
import TableRow from '@tiptap/extension-table-row';
import TableCell from '@tiptap/extension-table-cell';
import TableHeader from '@tiptap/extension-table-header';
import TextAlign from '@tiptap/extension-text-align';
import Placeholder from '@tiptap/extension-placeholder';
import { 
  Bold, Italic, Underline as UnderlineIcon, Heading1, Heading2, 
  Code, Image as ImageIcon, Table as TableIcon, Link as LinkIcon 
} from 'lucide-react';
import { Button } from '@/components/ui/button';

interface RichTextEditorProps {
  content: string;
  onChange: (content: string) => void;
  placeholder?: string;
}

export default function RichTextEditor({ 
  content, 
  onChange, 
  placeholder = 'Start typing...' 
}: RichTextEditorProps) {
  const editor = useEditor({
    extensions: [
      StarterKit,
      Underline,
      Link,
      Image,
      Table.configure({
        resizable: true,
      }),
      TableRow,
      TableHeader,
      TableCell,
      TextAlign.configure({
        types: ['heading', 'paragraph'],
      }),
      Placeholder.configure({
        placeholder,
      }),
    ],
    content,
    onUpdate: ({ editor }) => {
      onChange(editor.getHTML());
    },
  });

  if (!editor) {
    return null;
  }

  return (
    <div className="border rounded-lg overflow-hidden bg-white">
      {/* Toolbar */}
      <div className="flex items-center gap-1 p-2 border-b bg-gray-50">
        <Button
          type="button"
          variant="ghost"
          size="sm"
          onClick={() => editor.chain().focus().toggleBold().run()}
          className={editor.isActive('bold') ? 'bg-gray-200' : ''}
        >
          <Bold className="w-4 h-4" />
        </Button>
        
        <Button
          type="button"
          variant="ghost"
          size="sm"
          onClick={() => editor.chain().focus().toggleItalic().run()}
          className={editor.isActive('italic') ? 'bg-gray-200' : ''}
        >
          <Italic className="w-4 h-4" />
        </Button>
        
        <Button
          type="button"
          variant="ghost"
          size="sm"
          onClick={() => editor.chain().focus().toggleUnderline().run()}
          className={editor.isActive('underline') ? 'bg-gray-200' : ''}
        >
          <UnderlineIcon className="w-4 h-4" />
        </Button>
        
        <div className="w-px h-6 bg-gray-300 mx-1" />
        
        <Button
          type="button"
          variant="ghost"
          size="sm"
          onClick={() => editor.chain().focus().toggleHeading({ level: 1 }).run()}
          className={editor.isActive('heading', { level: 1 }) ? 'bg-gray-200' : ''}
        >
          <Heading1 className="w-4 h-4" />
        </Button>
        
        <Button
          type="button"
          variant="ghost"
          size="sm"
          onClick={() => editor.chain().focus().toggleHeading({ level: 2 }).run()}
          className={editor.isActive('heading', { level: 2 }) ? 'bg-gray-200' : ''}
        >
          <Heading2 className="w-4 h-4" />
        </Button>
        
        <div className="w-px h-6 bg-gray-300 mx-1" />
        
        <Button
          type="button"
          variant="ghost"
          size="sm"
          onClick={() => editor.chain().focus().toggleCodeBlock().run()}
          className={editor.isActive('codeBlock') ? 'bg-gray-200' : ''}
        >
          <Code className="w-4 h-4" />
        </Button>
        
        <Button
          type="button"
          variant="ghost"
          size="sm"
          onClick={() => {
            const url = window.prompt('Enter image URL');
            if (url) {
              editor.chain().focus().setImage({ src: url }).run();
            }
          }}
        >
          <ImageIcon className="w-4 h-4" />
        </Button>
        
        <Button
          type="button"
          variant="ghost"
          size="sm"
          onClick={() => editor.chain().focus().insertTable({ rows: 3, cols: 3 }).run()}
        >
          <TableIcon className="w-4 h-4" />
        </Button>
      </div>

      {/* Editor Content */}
      <EditorContent 
        editor={editor} 
        className="prose max-w-none p-4 min-h-[150px] focus:outline-none"
      />
    </div>
  );
}
```

---

## 🎨 **CSS for Tiptap (globals.css)**

```css
/* Tiptap Editor Styles */
.ProseMirror {
  outline: none;
}

.ProseMirror p.is-editor-empty:first-child::before {
  color: #adb5bd;
  content: attr(data-placeholder);
  float: left;
  height: 0;
  pointer-events: none;
}

.ProseMirror table {
  border-collapse: collapse;
  table-layout: fixed;
  width: 100%;
  margin: 1em 0;
  overflow: hidden;
}

.ProseMirror td,
.ProseMirror th {
  border: 2px solid #ced4da;
  padding: 8px;
  position: relative;
  vertical-align: top;
}

.ProseMirror th {
  background-color: #f1f3f5;
  font-weight: bold;
}

.ProseMirror h1 {
  font-size: 2em;
  font-weight: bold;
}

.ProseMirror h2 {
  font-size: 1.5em;
  font-weight: bold;
}

.ProseMirror code {
  background-color: #f8f9fa;
  padding: 2px 6px;
  border-radius: 4px;
  font-family: 'Courier New', monospace;
}

.ProseMirror pre {
  background-color: #1e1e1e;
  color: #d4d4d4;
  padding: 16px;
  border-radius: 8px;
  overflow-x: auto;
}

.ProseMirror pre code {
  background: none;
  padding: 0;
  color: inherit;
}
```

---

## 🚀 **Quick Installation**

### **Minimum Required Packages:**
```bash
# Core packages
npm install @tiptap/react @tiptap/starter-kit @tiptap/extension-underline @tiptap/extension-link @tiptap/extension-image @tiptap/extension-table @tiptap/extension-table-row @tiptap/extension-table-cell @tiptap/extension-table-header @tiptap/extension-placeholder

# Form management
npm install react-hook-form zod @hookform/resolvers

# Icons
npm install lucide-react
```

### **Optional but Recommended:**
```bash
# Drag and drop for reordering
npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities

# Math/LaTeX support
npm install @tiptap/extension-mathematics katex
```

---

## 📁 **Project Structure**

```
src/
├── app/
│   └── questions/
│       └── create/
│           └── page.tsx
├── components/
│   ├── ui/                    # shadcn components
│   │   ├── button.tsx
│   │   ├── input.tsx
│   │   ├── select.tsx
│   │   ├── radio-group.tsx
│   │   ├── card.tsx
│   │   └── label.tsx
│   ├── QuestionEditor.tsx     # Main editor component
│   └── RichTextEditor.tsx     # Tiptap wrapper
├── lib/
│   └── utils.ts
└── styles/
    └── globals.css
```

---

## ✅ **Features Implemented**

Based on the image you provided:

1. ✅ **Question Header Bar**
   - Question number input
   - Source field
   - Points input
   - Question type selector (MCQ/CQ/Written)
   - Delete button

2. ✅ **Question Title Input**
   - Large text input for question title

3. ✅ **Rich Text Editor**
   - Bold, Italic, Underline formatting
   - Heading styles (H1, H2)
   - Code blocks
   - Image insertion
   - Table insertion
   - Blue background section

4. ✅ **MCQ Options**
   - Radio button selection
   - Label (A, B, C, D)
   - Text input for each option
   - Delete button for extra options
   - Add option button (up to 5 options)

5. ✅ **Solution Section**
   - Separate rich text editor
   - Green background section

6. ✅ **Action Buttons**
   - ADD button
   - ADD 5 button
   - Save button

---

## 🎯 **Key Features**

- **Form Validation**: Uses Zod schema for type-safe validation
- **Dynamic Fields**: Add/remove options dynamically
- **Rich Text Editing**: Full WYSIWYG editor with toolbar
- **Responsive Design**: Works on all screen sizes
- **TypeScript Support**: Full type safety
- **Modern UI**: Clean, professional interface
- **Accessibility**: Proper ARIA labels and keyboard navigation

---

## 💡 **Advanced Features to Add**

```typescript
// Image upload handler
const handleImageUpload = async (file: File) => {
  const formData = new FormData();
  formData.append('image', file);
  
  const response = await fetch('/api/upload', {
    method: 'POST',
    body: formData,
  });
  
  const { url } = await response.json();
  return url;
};

// Drag and drop for options
import { DndContext, closestCenter } from '@dnd-kit/core';
import { arrayMove, SortableContext, useSortable } from '@dnd-kit/sortable';

// Auto-save functionality
import { useDebounce } from '@/hooks/useDebounce';

useEffect(() => {
  const debouncedSave = debounce(() => {
    // Auto-save logic
  }, 1000);
  
  debouncedSave();
}, [formData]);
```

---

## 🔗 **Useful Resources**

- [Tiptap Documentation](https://tiptap.dev/)
- [React Hook Form Docs](https://react-hook-form.com/)
- [shadcn/ui Components](https://ui.shadcn.com/)
- [Zod Validation](https://zod.dev/)
- [DND Kit](https://dndkit.com/)

---

This setup provides everything you need to build the exact UI shown in your image! 🚀